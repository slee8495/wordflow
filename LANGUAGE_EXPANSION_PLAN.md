# 다국어 확장 계획 (ko/en → ko/en/zh/ja/es)

## 배경

지금은 UI 언어(Settings)와 성경 본문 언어(Today/Reading 화면의 별도 토글)가 분리되어 있었음.
5개 언어(한/영/중/일/스)로 확장할 계획이라, 화면마다 따로 언어를 고르는 지금 구조는 맞지 않음 →
Settings에서 고른 언어 하나로 UI + 본문이 함께 바뀌도록 통일하기로 결정.

## 1단계 — 완료 (2026-08-05)

- [x] `page.tsx`/`reading/page.tsx`의 독립적인 `contentLanguage` state, `localStorage`
      (`wordflow:lang`), 토글 UI 제거
- [x] 본문 언어를 `useUiLanguage()`의 `uiLang`에서 직접 파생하도록 변경 (Settings에서 고른 언어가
      곧 본문 언어)
- [x] Settings의 `uiLanguageHint` 문구를 "메뉴만 바뀜" → "메뉴 + 본문 모두 바뀜"으로 수정
- [x] `npx tsc --noEmit`, `npm test` (20 tests), `next build` 모두 통과 확인

현재는 여전히 ko/en 두 언어만 지원 — 이번 작업은 UI 정리만이고, zh/ja/es 콘텐츠 생성 작업은
아직 시작 안 함.

## 2단계 — zh/ja/es 추가 (착수 전, 차근차근 진행 예정)

언어별로 하나씩 순서대로 추가. 매 언어마다:
1. 큐리큘럼 본문 생성 (`generateReading.ts` 파이프라인에 언어 필드 추가)
2. 개역/직역이 아닌 "쉬운 말로 재구성" 스타일 유지 (지금 한글/영어 방식과 동일한 톤)
3. TTS 음성 지원 확인 (AI Gateway voice 옵션에 해당 언어 있는지)
4. 찬양 추천은 아래 "찬양 추천 신뢰도" 기준을 따름 — 무리해서 다 채우지 않기

## 찬양 추천(worship-song) 신뢰도 기준

`src/lib/youtube.ts`의 필터링 구조(EXCLUDE_TERMS, 아티스트 화이트리스트, 언어 스크립트 매칭 등)
자체는 언어에 상관없이 잘 일반화되지만, **언어별 allowlist/denylist의 실제 정확도는 언어마다
다름** — 검증 안 된 언어는 이상한 결과(설교/이단/무관한 영상)를 걸러내지 못할 위험이 있음.

| 언어 | 신뢰도 | 방침 |
|---|---|---|
| 한국어 (ko) | 높음 | 기존대로 유지 |
| 영어 (en) | 높음 | 기존대로 유지 |
| 스페인어 (es) | 중간 | 추가 시도 가능, 초기엔 좁은 화이트리스트로 시작 |
| 중국어 (zh) | 낮음~중간 | 추가 시도 가능하지만 검증 필요, 처음엔 보수적으로 |
| 일본어 (ja) | 낮음 | **당장은 찬양 추천 기능 자체를 넣지 않는 것을 권장** — 잘못된 추천 위험이 검증 안 된 상태 |

원칙: 모든 언어에 찬양 추천을 억지로 다 넣지 않아도 됨. 신뢰 못하는 언어는 찬양 섹션을 아예
숨기는 것이 이상한 영상을 잘못 추천하는 것보다 낫다.
